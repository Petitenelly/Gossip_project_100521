<header>
  <%= link_to "Nous contacter", '/contact' %>
  <%= link_to "Notre équipe", '/team' %>

  <h1>Bienvenue dans The Gossip Tour !</h1>
</header>

<div class = "jumbotron">

		<h2>Viens nous confier un secret, ici il sera bien divulgué !</h2>

		<h3>Et hop, la liste des derniers potins :</h3>

       </div>

<section>
<div class="card-deck">
<%Gossip.all.each do |gossip|%>
<div class="col-sm-4 mb-3">
<div class="card">
  <div class="card-header"
  
  </div>
  <div class="card-body align-center">
    <blockquote class="blockquote mb-0">
      <p><%=gossip.title%></p>
      <footer class="blockquote-footer">
      <div class="row justify-content-between align-self-center mx-3">
        <p><%=User.find(gossip.user_id).first_name%> <%=User.find(gossip.user_id).last_name%></p>
    
      </div>
      </footer>
    </blockquote>
  </div>
</div>
</div>
<%end%>
</div>
</section>


